# Role manifest — Аудитор (R24)
# Source-of-truth: PACK-verification (VR.R.002), DP.ROLE.001 §3.2 (R24)
# Contract: roles/ROLE-CONTRACT.md

name: auditor
id: R24
type: agential
display_name: "Аудитор"

# Нет runner — вызывается по запросу или встроен в Day Open / стратегирование
# runner: scripts/auditor.sh

install:
  auto: true
  priority: 11
